function main() {
    // Only change code below this line

    // Komentar sa jednom linijom

    /* Komentar sa vise linija
    koji pokazuje da mozemo u vise linija pisati 
    primjer ovdje je sa  3 linije */

    // Only change code above this line
    }
    console.log(main());
    module.exports = main;